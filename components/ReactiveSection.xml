<component_def name="reactive-section">
    <data_model>
        <state id="reactive_open" type="string">true</state>
        <state id="user_first_name" type="string">Alex</state>
        <state id="user_last_name" type="string">Morgan</state>
        <state id="item_quantity" type="number">3</state>
        <state id="unit_price" type="number">29.99</state>
        <state id="reactive_log" type="string">Watchers ready...</state>

        <computed id="display_full_name" deps="user_first_name, user_last_name">
            return ($data.user_first_name || '') + ' ' + ($data.user_last_name || '');
        </computed>

        <computed id="calculated_total_price" deps="item_quantity, unit_price">
            var qty = Number($data.item_quantity) || 0;
            var price = Number($data.unit_price) || 0;
            return (qty * price).toFixed(2);
        </computed>
    </data_model>

    <watch path="user_first_name">
        <step action="SET_STATE">
            <path>data.reactive_log</path>
            <value>State Watcher [user_first_name]: Changed from '{prevValue}' to '{newValue}'</value>
        </step>
    </watch>

    <watch path="item_quantity">
        <step action="SET_STATE">
            <path>data.reactive_log</path>
            <value>State Watcher [item_quantity]: Quantity updated to {newValue}</value>
        </step>
    </watch>

    <collapse bind="data.reactive_open" title="⚡ Watch &amp; Computed State System (&lt;computed&gt;, &lt;watch&gt;)" class="border border-slate-800 rounded-3xl overflow-hidden shadow-xl bg-slate-900/90 backdrop-blur-md" header_class="w-full flex items-center justify-between p-5 bg-slate-900 border-b border-slate-800 font-extrabold text-white hover:bg-slate-800/80 cursor-pointer text-sm" body_class="p-6 flex flex-col gap-6">
        <flex direction="column" gap="16" class="w-full">
            <flex direction="row" align="center" justify="between" class="p-3.5 bg-indigo-500/10 rounded-2xl border border-indigo-500/20">
                <span class="text-xs font-bold text-indigo-300">Fine-grained reactive state dependencies with zero VDOM diffing overhead.</span>
                <span class="px-2.5 py-1 bg-indigo-500/20 text-indigo-300 font-extrabold text-[10px] rounded-lg uppercase tracking-wider border border-indigo-500/30">EUIXReactivePlugin</span>
            </flex>

            <!-- Inputs & Computed Grid -->
            <grid cols="2" gap="16" class="w-full grid-cols-1 md:grid-cols-2">
                <!-- User Profile Form Box -->
                <flex direction="column" gap="12" class="p-5 bg-slate-950/80 border border-slate-800 rounded-2xl">
                    <span class="text-xs font-black text-slate-400 uppercase tracking-wider">1. Live Inputs (Mutate State)</span>

                    <flex direction="column" gap="4">
                        <label class="text-[11px] font-bold text-slate-400">First Name</label>
                        <input bind="data.user_first_name" placeholder="First Name" class="px-3.5 py-2 bg-slate-900 border border-slate-800 rounded-xl text-xs font-semibold text-white focus:outline-none focus:border-indigo-500" />
                    </flex>

                    <flex direction="column" gap="4">
                        <label class="text-[11px] font-bold text-slate-400">Last Name</label>
                        <input bind="data.user_last_name" placeholder="Last Name" class="px-3.5 py-2 bg-slate-900 border border-slate-800 rounded-xl text-xs font-semibold text-white focus:outline-none focus:border-indigo-500" />
                    </flex>

                    <flex direction="row" gap="8">
                        <flex direction="column" gap="4" class="w-1/2">
                            <label class="text-[11px] font-bold text-slate-400">Quantity</label>
                            <input bind="data.item_quantity" type="number" min="1" class="px-3.5 py-2 bg-slate-900 border border-slate-800 rounded-xl text-xs font-semibold text-white focus:outline-none focus:border-indigo-500" />
                        </flex>
                        <flex direction="column" gap="4" class="w-1/2">
                            <label class="text-[11px] font-bold text-slate-400">Unit Price ($)</label>
                            <input bind="data.unit_price" type="number" step="0.01" class="px-3.5 py-2 bg-slate-900 border border-slate-800 rounded-xl text-xs font-semibold text-white focus:outline-none focus:border-indigo-500" />
                        </flex>
                    </flex>
                </flex>

                <!-- Memoized Computed Display Box -->
                <flex direction="column" gap="12" class="p-5 bg-slate-950/80 text-white border border-slate-800 rounded-2xl justify-between">
                    <flex direction="column" gap="8">
                        <span class="text-xs font-black text-indigo-400 uppercase tracking-wider">2. Memoized Computed Properties (&lt;computed&gt;)</span>

                        <flex direction="column" class="p-3.5 bg-slate-900 rounded-xl border border-slate-800">
                            <span class="text-[10px] text-slate-400 font-bold uppercase">Computed Full Name</span>
                            <span class="text-sm font-black text-white">{data.display_full_name}</span>
                        </flex>

                        <flex direction="column" class="p-3.5 bg-slate-900 rounded-xl border border-slate-800">
                            <span class="text-[10px] text-slate-400 font-bold uppercase">Calculated Total Price</span>
                            <span class="text-lg font-black text-emerald-400">${data.calculated_total_price}</span>
                        </flex>
                    </flex>

                    <span class="text-[10px] text-slate-400 italic">Computed properties automatically memoize and recompute ONLY when specified dependencies mutate!</span>
                </flex>
            </grid>

            <!-- Reactive Watcher Log Stream -->
            <flex direction="row" align="center" gap="8" class="p-3.5 bg-amber-500/10 border border-amber-500/20 rounded-2xl text-amber-400 text-xs font-bold">
                <span class="w-2.5 h-2.5 rounded-full bg-amber-400 animate-pulse shrink-0"></span>
                <span>{data.reactive_log}</span>
            </flex>
        </flex>
    </collapse>
</component_def>
