<component_def name="router-section">
    <data_model>
        <state id="active_tab">overview</state>
        <state id="active_project_id">1</state>
    </data_model>

    <flex direction="column" gap="24" class="w-full">
        <!-- Hero Header -->
        <flex class="p-6 sm:p-8 bg-slate-900/90 border border-slate-800 rounded-3xl shadow-xl flex-col sm:flex-row gap-6 items-start sm:items-center justify-between">
            <flex direction="column" gap="4">
                <flex direction="row" align="center" gap="8" class="flex-wrap">
                    <span class="px-3 py-1 bg-purple-500/15 text-purple-400 font-extrabold text-xs rounded-full border border-purple-500/20 uppercase tracking-wider">
                        🧭 EUIX Web Router Engine
                    </span>
                    <span class="text-xs text-slate-400 font-medium">React Router Equivalency for XML DSL</span>
                </flex>
                <h1 class="text-2xl sm:text-3xl font-black text-white tracking-tight">Declarative Routing, Outlets &amp; Data Loaders</h1>
                <p class="text-xs sm:text-sm text-slate-400 max-w-2xl">
                    Dynamic route parameters, nested layouts, asynchronous data loaders, action handlers and state synchronization built directly into XML.
                </p>
            </flex>
        </flex>

        <!-- Router Interactive Demo Showcase -->
        <grid class="grid-cols-1 lg:grid-cols-3 gap-6">
            <!-- Left: Route Links & Navigation -->
            <flex direction="column" gap="16" class="bg-slate-900/80 backdrop-blur-md p-6 rounded-3xl border border-slate-800 shadow-xl">
                <h2 class="text-sm font-bold text-white uppercase tracking-wider">Router Navigation</h2>
                <div class="flex flex-col gap-2">
                    <button class="w-full text-left px-4 py-3 rounded-xl border text-xs font-bold transition flex items-center justify-between cursor-pointer {data.active_project_id == '1' ? 'bg-purple-600 border-purple-500 text-white shadow-lg shadow-purple-600/30' : 'bg-slate-800/80 hover:bg-slate-800 border-slate-700 text-slate-300'}">
                        <on_click action="SET_STATE">
                            <path>data.active_project_id</path>
                            <value>1</value>
                        </on_click>
                        <span>🚀 Next-Gen Core Engine</span>
                        <span class="text-[10px] opacity-75">/projects/1</span>
                    </button>

                    <button class="w-full text-left px-4 py-3 rounded-xl border text-xs font-bold transition flex items-center justify-between cursor-pointer {data.active_project_id == '2' ? 'bg-purple-600 border-purple-500 text-white shadow-lg shadow-purple-600/30' : 'bg-slate-800/80 hover:bg-slate-800 border-slate-700 text-slate-300'}">
                        <on_click action="SET_STATE">
                            <path>data.active_project_id</path>
                            <value>2</value>
                        </on_click>
                        <span>🗺️ Spatial Leaflet Studio</span>
                        <span class="text-[10px] opacity-75">/projects/2</span>
                    </button>

                    <button class="w-full text-left px-4 py-3 rounded-xl border text-xs font-bold transition flex items-center justify-between cursor-pointer {data.active_project_id == '3' ? 'bg-purple-600 border-purple-500 text-white shadow-lg shadow-purple-600/30' : 'bg-slate-800/80 hover:bg-slate-800 border-slate-700 text-slate-300'}">
                        <on_click action="SET_STATE">
                            <path>data.active_project_id</path>
                            <value>3</value>
                        </on_click>
                        <span>🛡️ Resilient API Pipeline</span>
                        <span class="text-[10px] opacity-75">/projects/3</span>
                    </button>
                </div>

                <div class="mt-4 pt-4 border-t border-slate-800 flex flex-col gap-2">
                    <span class="text-xs font-bold text-slate-400">Tabs:</span>
                    <flex direction="row" gap="2">
                        <button class="flex-1 py-1.5 rounded-lg text-xs font-bold transition {data.active_tab == 'overview' ? 'bg-indigo-600 text-white' : 'bg-slate-800 text-slate-400 hover:text-white'}">
                            <on_click action="SET_STATE">
                                <path>data.active_tab</path>
                                <value>overview</value>
                            </on_click>
                            Overview
                        </button>
                        <button class="flex-1 py-1.5 rounded-lg text-xs font-bold transition {data.active_tab == 'tasks' ? 'bg-indigo-600 text-white' : 'bg-slate-800 text-slate-400 hover:text-white'}">
                            <on_click action="SET_STATE">
                                <path>data.active_tab</path>
                                <value>tasks</value>
                            </on_click>
                            Tasks
                        </button>
                        <button class="flex-1 py-1.5 rounded-lg text-xs font-bold transition {data.active_tab == 'settings' ? 'bg-indigo-600 text-white' : 'bg-slate-800 text-slate-400 hover:text-white'}">
                            <on_click action="SET_STATE">
                                <path>data.active_tab</path>
                                <value>settings</value>
                            </on_click>
                            Settings
                        </button>
                    </flex>
                </div>
            </flex>

            <!-- Middle: Active Route Outlet View -->
            <flex direction="column" gap="16" class="lg:col-span-2 bg-slate-900/80 backdrop-blur-md p-6 rounded-3xl border border-slate-800 shadow-xl">
                <flex direction="row" justify="between" align="center">
                    <span class="text-xs font-bold text-purple-400 uppercase tracking-wider">Dynamic Outlet View</span>
                    <span class="text-xs font-mono text-slate-400">Route: /projects/{data.active_project_id}/{data.active_tab}</span>
                </flex>

                <div class="p-6 bg-slate-950/80 rounded-2xl border border-slate-800 flex flex-col gap-4">
                    <if condition="{data.active_project_id} == '1'">
                        <h2 class="text-xl font-black text-white">🚀 Next-Gen Core UI Engine</h2>
                        <p class="text-xs text-slate-300 leading-relaxed">
                            Ultra-lightweight declarative framework built around XML specifications with fine-grained reactivity and zero Virtual DOM overhead.
                        </p>
                    </if>

                    <if condition="{data.active_project_id} == '2'">
                        <h2 class="text-xl font-black text-white">🗺️ Spatial Leaflet Studio</h2>
                        <p class="text-xs text-slate-300 leading-relaxed">
                            Vector mapping suite with live geodesic polygon calculation and Cloudflare D1 SQL integration.
                        </p>
                    </if>

                    <if condition="{data.active_project_id} == '3'">
                        <h2 class="text-xl font-black text-white">🛡️ Resilient API Pipeline</h2>
                        <p class="text-xs text-slate-300 leading-relaxed">
                            Self-healing REST client with exponential backoff, circuit-breaker mechanisms, and offline queueing.
                        </p>
                    </if>

                    <div class="p-3 bg-slate-900/90 rounded-xl border border-slate-800/80 font-mono text-xs text-slate-300 flex justify-between items-center">
                        <span class="text-slate-400">Tab Content:</span>
                        <span class="text-emerald-400 font-bold uppercase">{data.active_tab} TAB ACTIVE</span>
                    </div>
                </div>
            </flex>
        </grid>
    </flex>
</component_def>
